#!/bin/sh

cd $(dirname $0)/..

if [ -r inventory ]; then
  exit 0
fi

read -p "Server IP/hostname: " host
echo $host > inventory

mkdir -p group_vars; cp bin/all.yml group_vars
read -p "Copied sample to group_vars/all.yml - Edit this and hit return> "

make bootstrap
